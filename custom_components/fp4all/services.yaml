update_history:
  name: Update history
  description: Download the history table from the FP4All logger.
  fields:
    entity_id:
      name: Entity
      description: One sensor entity of the inverter.
      required: true
      selector:
        entity:
          domain: sensor